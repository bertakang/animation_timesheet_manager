<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:animationtimelinetool.Controls"
             x:Class="animationtimelinetool.MainPage">
             <!-- dotnet build -t:Run -f net9.0-maccatalyst -p:HotReloadEnabled=true -->
    <ScrollView>
        <Grid Margin="20,35,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>

                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

<local:FileMenuButton Grid.Row="0" Grid.Column="0" />
<local:FileMenuButton Grid.Row="0" Grid.Column="1" />
<local:FileMenuButton Grid.Row="0" Grid.Column="2" />
<local:FileMenuButton Grid.Row="1" Grid.Column="0" />
<local:FileMenuButton Grid.Row="1" Grid.Column="1" />
<local:FileMenuButton Grid.Row="1" Grid.Column="2" />

        </Grid>
    </ScrollView>
</ContentPage>
